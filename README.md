# homebrew-supabase-shim-poc

Homebrew tap used to validate the TypeScript CLI release pipeline
(see dx-lab ADR 0011). Non-production, do not install in production.

Install:
```
brew tap avallete/supabase-shim-poc
brew install supabase-shim-poc
```

